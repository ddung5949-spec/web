// Clean mock data file with zero mock or hardcoded admin accounts
import { User, MilitaryProfile } from '../types';

export const mockUsers: User[] = [];
export const mockAdminUser: User | null = null;
export const mockMilitaryProfiles: MilitaryProfile[] = [];
